<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse nav-justified navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if (can? :do_as_student, :all || !user_signed_in?) %>
          <%= link_to 'Home', root_path, class: 'navbar-brand' %>
      <% end %>
      <% if ((can? :do_as_admin, :all) || (can? :do_as_superadmin, :all))%>
      <%= link_to 'Admin Home', root_path, class: 'navbar-brand' %>
      <% end %>
      <% if can? :do_as_instructor, :all %>
          <%= link_to 'Instructor Home', root_path, class: 'navbar-brand' %>
      <% end %>

      </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
        <%= render 'layouts/nav_links_for_auth' %>
      </ul>
    </div>
  </div>
</nav>
